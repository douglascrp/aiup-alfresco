---
name: content-store
description: Scaffold a custom Alfresco ContentStore connector (extending AbstractContentStore, optionally wrapping a caching or encrypting store) with its reader/writer, Spring wiring, and unit test. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: content-store

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/content-store.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/content-store` as `$ARGUMENTS` from the command spec.
