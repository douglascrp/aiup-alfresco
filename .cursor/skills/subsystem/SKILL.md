---
name: subsystem
description: Scaffold a custom Alfresco subsystem (ChildApplicationContextFactory + default/instance properties) or, in authentication mode, configure a custom authentication chain / external IdP (LDAP, identity-service/OIDC, external). In-Process SDK (Maven) / config.
disable-model-invocation: true
---

# AIUP command: subsystem

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/subsystem.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/subsystem` as `$ARGUMENTS` from the command spec.
