---
name: behaviours
description: Scaffold Alfresco behaviour/policy classes with Spring bean wiring. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: behaviours

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/behaviours.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/behaviours` as `$ARGUMENTS` from the command spec.
