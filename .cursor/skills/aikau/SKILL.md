---
name: aikau
description: Generate Share Aikau page and dashlet artefacts for legacy Share UI customizations.
disable-model-invocation: true
---

# AIUP command: aikau

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/aikau.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/aikau` as `$ARGUMENTS` from the command spec.
