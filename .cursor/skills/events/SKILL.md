---
name: events
description: Generate an Out-of-Process Spring Boot event listener for Alfresco Java Event API.
disable-model-invocation: true
---

# AIUP command: events

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/events.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/events` as `$ARGUMENTS` from the command spec.
