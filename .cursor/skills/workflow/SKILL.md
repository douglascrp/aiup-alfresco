---
name: workflow
description: Generate Activiti BPMN 2.0 process definition, workflow task content model, Spring bootstrap registration, i18n message bundle, and optional Java task listener. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: workflow

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/workflow.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/workflow` as `$ARGUMENTS` from the command spec.
