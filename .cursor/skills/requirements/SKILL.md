---
name: requirements
description: Gather and structure requirements for an Alfresco extension as user stories with acceptance criteria.
disable-model-invocation: true
---

# AIUP command: requirements

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/requirements.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/requirements` as `$ARGUMENTS` from the command spec.
