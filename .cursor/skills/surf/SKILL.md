---
name: surf
description: Generate Share Surf extension artefacts for legacy Share pages, components, templates, and extension metadata.
disable-model-invocation: true
---

# AIUP command: surf

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/surf.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/surf` as `$ARGUMENTS` from the command spec.
