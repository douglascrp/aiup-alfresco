---
name: scheduled-jobs
description: Generate a cluster-safe Quartz scheduled job with configurable cron expression, separate executer bean, Spring scheduler registration, and unit test. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: scheduled-jobs

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/scheduled-jobs.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/scheduled-jobs` as `$ARGUMENTS` from the command spec.
