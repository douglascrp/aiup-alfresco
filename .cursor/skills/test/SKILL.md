---
name: test
description: Generate integration tests for Alfresco extensions using Testcontainers (self-contained, no pre-running ACS required).
disable-model-invocation: true
---

# AIUP command: test

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/test.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/test` as `$ARGUMENTS` from the command spec.
