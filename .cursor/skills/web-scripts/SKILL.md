---
name: web-scripts
description: Generate Alfresco Web Script descriptors, controllers, and FreeMarker templates. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: web-scripts

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/web-scripts.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/web-scripts` as `$ARGUMENTS` from the command spec.
