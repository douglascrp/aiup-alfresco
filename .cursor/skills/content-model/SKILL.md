---
name: content-model
description: Generate Alfresco content model XML and Spring context file from requirements.
disable-model-invocation: true
---

# AIUP command: content-model

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/content-model.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/content-model` as `$ARGUMENTS` from the command spec.
