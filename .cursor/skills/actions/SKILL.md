---
name: actions
description: Scaffold Alfresco ActionExecuter classes with Spring bean registration. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: actions

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/actions.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/actions` as `$ARGUMENTS` from the command spec.
