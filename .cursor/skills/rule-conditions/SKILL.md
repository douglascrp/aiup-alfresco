---
name: rule-conditions
description: Generate a custom Alfresco rule condition evaluator with parameter definitions, Spring bean registration, and unit test. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: rule-conditions

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/rule-conditions.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/rule-conditions` as `$ARGUMENTS` from the command spec.
