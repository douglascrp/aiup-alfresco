---
name: metadata-extractor
description: Scaffold a custom Alfresco metadata extractor/embedder (extending AbstractMappingMetadataExtracter) with a properties mapping file, Spring registration, and unit test. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: metadata-extractor

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/metadata-extractor.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/metadata-extractor` as `$ARGUMENTS` from the command spec.
