---
name: audit
description: Generate a custom Alfresco audit application (audit XML + data extractors/generators + enable properties) and optional query Web Script. In-Process SDK (Maven) only.
disable-model-invocation: true
---

# AIUP command: audit

1. Read `AGENTS.md`.
2. Execute the full procedure in `commands/audit.md`.
3. Apply referenced validator skills under `.cursor/skills/` when the command mentions them.
4. Create or update real files unless the user asked for a plan only.

Treat any user text after `/audit` as `$ARGUMENTS` from the command spec.
